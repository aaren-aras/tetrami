# tetrami

A C++ **Swiss Army Knife** for medical imaging.

Formats:
- DICOM
- NIfTI

Goals:
- fast native desktop app
- cross-platform (Windows / Linux / macOS)
- viewer, converter, metadata inspector
- future AI inference hooks

Build:

```bash
mkdir build
cd build
cmake ..
cmake --build .
